DATABASE_URL=postgresql://migrate_workflow:migrate_workflow@127.0.0.1:55433/migrate_workflow
MIGRATE_SERVER_TOKEN=local-migrate-secret
CRON_SECRET=local-cron-secret

# Browser TUI deployment. Create this with `pnpm sandbox:snapshot`.
# MIGRATE_TUI_SANDBOX_SNAPSHOT_ID=snap_xxxxxxxxxxxx
# Required when MIGRATE_TUI_SANDBOX_SNAPSHOT_ID is set.
MIGRATE_SERVER_PUBLIC_URL=https://YOUR_DEPLOYMENT/api/migrate
