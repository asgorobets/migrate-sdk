---
"migrate-sdk": minor
---

Let each migration definition declare one optional group, and select the
aggregated group through the SDK or `run`, `rollback`, and `status` CLI commands
with `--group`.
